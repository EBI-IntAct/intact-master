layout to finish (table with display:)

CRON tab for stat update

Oracle to test

Make the build.xml using intact-core.jar

stat-context.xml

Legend: timestamp would be useful

Fix bug: sometimes different display of graph during the two first run ;o(
